---
import { EVENT_DATE_ISO } from "../data/config";
---

<div
  class="grid max-w-[460px] grid-cols-4 gap-2.5"
  id="countdown"
  data-target={EVENT_DATE_ISO}
  aria-live="polite"
>
  <div
    class="rounded-brand-sm border border-border bg-white/5 px-1 py-3 text-center backdrop-blur-md"
  >
    <div
      class="font-display text-[clamp(22px,6vw,30px)] tabular-nums text-foreground"
      data-unit="days"
    >
      --
    </div>

    <div
      class="mt-1 text-[10.5px] uppercase tracking-[0.08em] text-foreground-muted"
    >
      Días
    </div>
  </div>

  <div
    class="rounded-brand-sm border border-border bg-white/5 px-1 py-3 text-center backdrop-blur-md"
  >
    <div
      class="font-display text-[clamp(22px,6vw,30px)] tabular-nums text-foreground"
      data-unit="hours"
    >
      --
    </div>

    <div
      class="mt-1 text-[10.5px] uppercase tracking-[0.08em] text-foreground-muted"
    >
      Horas
    </div>
  </div>

  <div
    class="rounded-brand-sm border border-border bg-white/5 px-1 py-3 text-center backdrop-blur-md"
  >
    <div
      class="font-display text-[clamp(22px,6vw,30px)] tabular-nums text-foreground"
      data-unit="minutes"
    >
      --
    </div>

    <div
      class="mt-1 text-[10.5px] uppercase tracking-[0.08em] text-foreground-muted"
    >
      Minutos
    </div>
  </div>

  <div
    class="rounded-brand-sm border border-border bg-white/5 px-1 py-3 text-center backdrop-blur-md"
  >
    <div
      class="font-display text-[clamp(22px,6vw,30px)] tabular-nums text-foreground"
      data-unit="seconds"
    >
      --
    </div>

    <div
      class="mt-1 text-[10.5px] uppercase tracking-[0.08em] text-foreground-muted"
    >
      Segundos
    </div>
  </div>
</div>

<script>
  function initCountdown() {
    const el = document.getElementById("countdown");
    if (!el) return;

    const targetISO = el.dataset.target;
    if (!targetISO) return;

    const target = new Date(targetISO).getTime();

    const daysEl = el.querySelector('[data-unit="days"]');
    const hoursEl = el.querySelector('[data-unit="hours"]');
    const minutesEl = el.querySelector('[data-unit="minutes"]');
    const secondsEl = el.querySelector('[data-unit="seconds"]');

    function pad(n: number) {
      return n.toString().padStart(2, "0");
    }

    function tick() {
      const now = Date.now();
      const diff = target - now;

      if (diff <= 0) {
        el.innerHTML = `
          <div class="col-span-4 font-display text-[clamp(20px,5vw,26px)] text-primary">
            ¡Llegó el momento! 🇦🇷
          </div>
        `;

        clearInterval(timer);
        return;
      }

      const days = Math.floor(diff / (1000 * 60 * 60 * 24));
      const hours = Math.floor((diff / (1000 * 60 * 60)) % 24);
      const minutes = Math.floor((diff / (1000 * 60)) % 60);
      const seconds = Math.floor((diff / 1000) % 60);

      if (daysEl) daysEl.textContent = String(days);
      if (hoursEl) hoursEl.textContent = pad(hours);
      if (minutesEl) minutesEl.textContent = pad(minutes);
      if (secondsEl) secondsEl.textContent = pad(seconds);
    }

    tick();

    const timer = setInterval(tick, 1000);
  }

  initCountdown();
  document.addEventListener("astro:page-load", initCountdown);
</script>